# ── TypeScript / JavaScript: Biome (lint + format) ────────────────────
- id: biome
  name: biome (lint + format)
  entry: bash
  args:
    - -c
    - |
      if [ "$LINT_MODE" = "check" ]; then
        npx --yes @biomejs/biome@2.2.4 check --error-on-warnings "$@"
      else
        npx --yes @biomejs/biome@2.2.4 check --write "$@"
      fi
    - biome
  language: system
  files: '\.[cm]?[jt]sx?$'
  pass_filenames: true
